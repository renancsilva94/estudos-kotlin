fun main(){
    val num = 8

    when {
       num > 0 -> { println("numero é positivo") }
        num < 0 -> { println("numero é negativo") }
        else -> { println("ZERO")}
        }

    }
